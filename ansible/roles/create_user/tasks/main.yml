- name: Создаём пользователя {{ user_name }}
  ansible.builtin.user:
    name: "{{ user_name }}"
    state: present
    shell: /bin/bash
    create_home: true
    groups:
      - sudo
      - docker
    append: true

- name: Кладём публичный ключ в authorized_keys для пользователя {{ user_name }}
  ansible.posix.authorized_key:
    user: "{{ user_name }}"
    state: present
    key: "{{ lookup('file', lookup('env', 'HOME') + '/.ssh/id_ed25519.pub') }}"

- name: Настраиваем беспарольный sudo для пользователя {{ user_name }}
  ansible.builtin.copy:
    dest: /etc/sudoers.d/{{ user_name }}
    content: '{{ user_name }} ALL=(ALL) NOPASSWD: ALL'
    mode: '0440'
    validate: '/usr/sbin/visudo -cf %s'

- name: Запрещаем вход по паролю для всех пользователей
  ansible.builtin.lineinfile:
    path: /etc/ssh/sshd_config
    regexp: '^#?\s*PasswordAuthentication'
    line: 'PasswordAuthentication no'
    backup: true
    validate: '/usr/sbin/sshd -t -f %s'
  notify: restart ssh